/**
 * Main package of RPLidar4j
 *
 * The package has beend designed to model the domain about the sensor RPLidar A1.
 *
 * The way to use the package is easy, the package ev3dev.sensors.slamtec
 * provides a class to use the 2D LIDAR RPLidar A1.
 *
 * final String USBPort = "/dev/ttyUSB0";
 * final RPLidarA1 lidar = new RPLidarA1(USBPort);
 * lidar.init();
 * lidar.scan();
 * lidar.close();
 *
 */
package ev3dev.sensors.slamtec;